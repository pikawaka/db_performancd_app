<div id="<%= dom_id book %>">
  <p>
    <strong>Title:</strong>
    <%= book.title %>
  </p>

  <p>
    <strong>Author:</strong>
    <%= book.author.name %>
  </p>

  <ul class="reviews-list">
    <% book.reviews.each do |review| %>
      <li><%= review.content %></li>
    <% end %>
  </ul>
</div>
